# Robots

Owns the mock payer portal fallback robot.

Current setup caveat: Assistant/Robot is installed, but folder-specific runtime allocation must be resolved before this checkpoint.

Use `.agents/skills/uipath-rpa/SKILL.md` before authoring RPA artifacts.
