# API Workflows

Owns UiPath API Workflow calls for:

- mock EHR pull
- event mirror write
- payer submission
- payer decision fetch
- pharmacy/scheduling handoff

Use `.agents/skills/uipath-api-workflow/SKILL.md` before authoring workflow files.
