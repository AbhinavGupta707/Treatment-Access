# Local Agent Skills

This project uses UiPath coding-agent skills locally. They are intentionally not committed because they are generated tool content.

Regenerate them from the project root:

```bash
uip skills install --agent codex --local
```

Expected local path:

```text
.agents/skills
```
